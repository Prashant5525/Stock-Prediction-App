python -m venv venv
venv\Scripts\activate
pip install streamlit pandas numpy matplotlib yfinance scikit-learn keras
python.exe -m pip install --upgrade pip
pip install tensorflow
streamlit run app.py